# This class takes the input data and stores it

# import RPi.GPIO as gpio

#implement SPI interface with the ADC and set up the call function

class AnalogData:
    def ___init___(self, Xpin, Ypin, Vpin):
        # gpio.setup(Xpin, gpio.OUT);
        # gpio.setup(Ypin, gpio.OUT);
        # gpio.setup(Vpin, gpio.OUT);
        super();

    def givedata(self):
        # return read(Xpin), read(Ypin), read(Vpin) or something
        return 0;
